# 팀 소개 및 역할 분담

> **사람인 척 · humanish** — 3D 공간에 모여 대화하고, 누가 AI인지 찾아내는 웹 소셜 추리 게임

이 문서의 편집본(A4 3쪽)은 [`team-intro.html`](team-intro.html) 이다. 브라우저에서 열고 인쇄(⌘P) → PDF 로 저장하면 배포용 PDF가 나온다.

| 팀 구성 | 개발 기간 | 전체 커밋 |
|---|---|---|
| 3 명 | 13 일 · 07.27 – 08.08 | 232 개 |

| 화면 | 3D · 실시간 | 서버 · 데이터 | 배포 · AI |
|---|---|---|---|
| Next.js 15 · React 19 · TypeScript | Three.js · Durable Objects | Supabase Postgres · RLS | Cloudflare Workers · NVIDIA NIM |

---

## 1. 역할 분담 요약 (At a glance)

| 팀원 | 역할 | 담당 영역 | 커밋 |
|---|---|---|---|
| **이유경** (YuGyeong) | 서버 · 데이터베이스<br>게임 규칙 설계 | DB 스키마와 접근 권한, 게임 진행 상태머신, 쓰기 API 전부, 규칙 문서(SPEC), 대기실·게임방 화면, 계정과 전적, 테스트 체계 | **117** (50%) |
| **노원상** (nowonsang) | 3D 월드 · 실시간 통신<br>배포 · 비주얼 | Three.js 3D 공간, 실시간 멀티플레이 서버, 아바타와 애니메이션, 월드 안의 게임 진행, Cloudflare 배포, 아트 디렉션 | **65** (28%) |
| **hbkim507** (hbkim507) | AI 에이전트<br>LLM 연동 | 봇 페르소나와 프롬프트, LLM 연동과 보안 게이트, 모델 실험실, 봇 응답 재생성, 연기자 규칙 구현 | **50** (22%) |

담당은 사람이 아니라 **계층으로 나눴다** — 서버 · 규칙 · 화면 · 3D 월드가 각각 다른 폴더에 있다. 수치는 `git log` 실측값.

---

## 2. 기여 분포 (Contribution)

**커밋 수 · 총 232개**

| 팀원 | 커밋 | 비율 |
|---|---|---|
| 이유경 | 117 | 50% |
| 노원상 | 65 | 28% |
| hbkim507 | 50 | 22% |

**날짜별 커밋 · 13일** — 07.27 설계 확정 → 08.03 3D 월드에 게임 결합 → 08.08 마감 (최다 커밋일은 08.07)

커밋 수는 기여의 **모양**일 뿐 크기가 아니다 — AI 에이전트 영역은 커밋이 가장 적지만 가장 자주 되돌아온 곳이고, 그 폴더만 테스트가 커밋의 3분의 1이다.

---

## 3. 팀원별 담당과 핵심 기여 (Responsibilities)

### 이유경 — 서버 · 데이터베이스 / 게임 규칙 설계

`lyg64522` · 117 commits

**담당 코드** — `supabase/` · `lib/server/` · `app/api/` · `docs/SPEC.md` · `components/` · `app/main/` · `lib/auth/` · `tests/`

**핵심 기여**

- **게임 진행 상태머신** — 페이즈 전환을 DB 함수로 만들고 시각 판정을 서버 시계로 통일했다.
- **정답을 숨기는 구조** — 클라이언트엔 원본 대신 가공된 뷰만. 자리가 새면 게임이 즉시 끝난다.
- **규칙 확정과 문서화** — 진영 · 승패 · 투표 규칙을 SPEC §18로 못 박아 기준을 하나로 만들었다.
- **쓰기 API 전부** — 방 생성 · 입장 · 시작 · 답변 · 투표. 서버가 신원을 다시 확인한 뒤에만 쓴다.
- **계정과 전적** — 익명으로 시작해 구글 계정으로 잇고, 지난 판을 기록으로 남긴다.
- **대기실 · 게임방 화면과 테스트 체계** — DB는 흉내가 아니라 실제 Postgres로 검사한다.

### 노원상 — 3D 월드 · 실시간 통신 / 배포 · 비주얼

`nowonsang` · 65 commits

**담당 코드** — `app/world/` · `worker/` · `lib/mp/` · `app/bg-3d/` · `app/intro/` · `wrangler.jsonc` · `public/`

**핵심 기여**

- **3D 월드 맵** — Three.js 창고 라운지. 여기서 뽑은 조명·재질이 화면 전체의 색이 됐다.
- **실시간 멀티플레이** — 방 하나를 서버 객체 하나로 두고, 8인의 좌표를 초당 10회 주고받는다.
- **월드 안의 게임 진행** — 주제 발언 · 투표 · 최후변론 · 처형 · 재투표를 3D 공간에서 돌게 했다.
- **아바타와 연출** — 걷기·점프 애니메이션, 말풍선, 스크린 영상, 배경음악, 주제 안내 음성.
- **배포 파이프라인** — Cloudflare 워커 두 개를 잇고 설정값을 한 곳에 모아 키를 잠갔다.

### hbkim507 — AI 에이전트 / LLM 연동

`hbkim507` · 50 commits

**담당 코드** — `lib/agent/` · `app/api/agent/` · `app/lab/` · `lib/server/world-ai.ts` · `lib/game/rules.ts` · `tests/lib/agent/`

**핵심 기여**

- **봇이 사람처럼 말하게 하는 계층** — 페르소나 · 말투 위장 · 자백 차단.
- **LLM 연동과 보안** — 키는 서버에서만 읽고 호출 경로를 하나로 좁혔다. 인증 없는 요청은 차단.
- **모델 실험실과 규칙 구현** — 모델 선택의 근거를 남기고, AI인 척하는 **연기자**와 승패 판정을 붙였다.

---

## 4. 각자의 대표 작업 (Key commits)

| 팀원 | 대표 작업 (실제 커밋 기준) | 시점 |
|---|---|---|
| **이유경** | 게임 진행 상태머신 — 중복 전환을 막는 잠금 방식까지 | 07.27 |
| | 방 생성 · 입장 · 시작 · 답변 · 투표 쓰기 API | 07.27 |
| | DB 접근 권한 구멍 차단 — 원본 테이블에 닿던 경로를 막음 | 07.27 |
| | 익명으로 시작해 구글 계정으로 잇는 로그인과 전적 | 08.01 |
| | 진영 · 승패 · 투표 규칙을 SPEC §18로 확정 | 08.02 |
| | 시작할 때 AI도 좌석 순열에 포함 — 번호로 AI를 못 찍게 | 08.08 |
| **노원상** | 3D 멀티플레이 — 방 하나 = 서버 객체 하나 | 07.28 |
| | 워커 두 개를 공개 주소로 잇는 배포 구성 | 07.29 |
| | 아바타 애니메이션 · 스크린 영상 · 배경 음악 | 07.31 |
| | 3D 월드에 판을 붙임 — 주제 · 투표 · 최후변론 · 처형 | 08.03 |
| | 부결되면 지목부터 다시 — 재투표와 로비 복귀 후 재입장 | 08.06 |
| **hbkim507** | 봇 페르소나 · 말투 위장 · 자백 차단 계층 | 07.29 |
| | LLM 어댑터와 모델 비교 실험실 | 07.29 |
| | 운영 환경 인증 게이트 — 인증 없는 호출은 차단 | 07.31 |
| | 연기자 — AI인 척하는 사람과 승패 판정 | 08.04 |

---

## 5. 협업 방식 (How we worked)

**01 · 규칙 문서 하나가 유일한 기준**
게임 규칙은 `docs/SPEC.md` 하나에만 적고, **코드와 어긋나면 문서가 맞다**. 섹션 번호를 코드 주석이 직접 참조해 규칙이 바뀌면 고칠 코드가 검색 한 번에 나온다.

**02 · 어기면 게임이 깨지는 열 가지**
팀 규칙을 `CLAUDE.md` 에 열 개만 못 박았다. 예: **AI의 자리를 밖으로 내보내지 않는다**, 진행 판정에 클라이언트 시계를 쓰지 않는다, 쓰기는 전부 서버를 거친다.

**03 · 기능별 브랜치와 PR**
기능마다 브랜치를 파고 리뷰 후 합쳤다. 커밋 메시지는 「무엇을 왜」를 **한국어 한 줄로** 쓰고 근거 섹션을 붙여, 히스토리만 읽어도 결정 이유를 따라갈 수 있다.

**04 · 검사를 사람 눈에 맡기지 않음**
규칙과 화면은 자동 테스트로, DB 동작은 **흉내가 아니라 실제 Postgres에** 물어본다. 멀티플레이는 배포 후 소켓 두 개를 실제로 왕복시켜 확인한다.
