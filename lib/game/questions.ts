/**
 * 질문 풀. 소유: B (SPEC §2)
 *
 * 목표 30개. 사람과 AI의 답이 갈리는 질문이 좋은 질문이다.
 * 순수 데이터 — DB · 네트워크 접근 금지.
 */

/** question 페이즈에서 전원이 같이 답하는 질문 (SPEC §5.3). */
export const COMMON_QUESTIONS: readonly string[] = [
  // TODO(B): 30개까지 채운다.
];

/** target 페이즈에서 특정 플레이어를 지목해 던지는 질문. */
export const TARGET_QUESTIONS: readonly string[] = [
  // TODO(B)
];
